import { StyleSheet } from "react-native";
import { darkColor } from "../../style/theme";

export const styles = StyleSheet.create({
    title: {
        fontWeight: 500,
        fontSize: 22,
        marginTop: 12
    },
    text: {
        color: "#D4D4D8"
    },
    cardRenda: {
        backgroundColor: "#18181B",
        padding: 6,
        paddingHorizontal: 10,
        borderRadius: 12,
        marginVertical: 12,
        borderColor: "#27272A",
        borderWidth: 1
    }
})